# ---------------------------------------------------------------------------
# Production-pinned dependencies for the Election Process Assistant
# ---------------------------------------------------------------------------

# Web framework
fastapi==0.111.1

# ASGI server (production-grade, used inside Docker)
uvicorn[standard]==0.30.1

# Async HTTP client (used for Civic API requests)
httpx==0.27.0

# Data validation
pydantic==2.7.4

# Environment variable management (optional .env support)
python-dotenv==1.0.1

# ---------------------------------------------------------------------------
# Testing (not installed in production image – excluded in Dockerfile)
# ---------------------------------------------------------------------------
pytest==8.2.2
pytest-asyncio==0.23.7
respx==0.21.1          # httpx request mocking
