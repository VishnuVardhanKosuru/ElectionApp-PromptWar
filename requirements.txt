# ---------------------------------------------------------------------------
# Production-pinned dependencies for the Election Process Assistant v2.1
# ---------------------------------------------------------------------------

# Web framework
fastapi==0.111.1

# ASGI server (production-grade)
uvicorn[standard]==0.30.1

# Production process manager (Gunicorn + UvicornWorker for concurrency)
gunicorn==22.0.0

# Async HTTP client (used for Civic API requests)
httpx==0.27.0

# Data validation (Pydantic v2)
pydantic==2.7.4

# Pydantic settings for environment variable management
pydantic-settings==2.3.4

# Environment variable management (.env file support)
python-dotenv==1.0.1

# ---------------------------------------------------------------------------
# Caching
# ---------------------------------------------------------------------------
# In-memory TTL + LRU cache (Civic API response caching)
cachetools==5.3.3

# ---------------------------------------------------------------------------
# Google Cloud Integrations (all required – graceful fallback in local dev)
# ---------------------------------------------------------------------------

# Cloud Logging – structured log export to Cloud Console
google-cloud-logging==3.10.0

# Secret Manager – secure API key retrieval at runtime
google-cloud-secret-manager==2.20.0

# Firestore – persistent NoSQL incident report storage
google-cloud-firestore==2.16.1

# Cloud Storage – PDF export archival
google-cloud-storage==2.17.0

# ---------------------------------------------------------------------------
# PDF Export
# ---------------------------------------------------------------------------
fpdf2==2.7.9

# ---------------------------------------------------------------------------
# Testing (excluded from production image via Dockerfile grep filter)
# ---------------------------------------------------------------------------
pytest==8.2.2
pytest-asyncio==0.23.7
respx==0.21.1
pytest-cov==5.0.0
